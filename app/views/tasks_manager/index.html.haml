%h2 Tasks manager

%table.table
  %thead
    %tr
      %th Task
      %th Description
      %th Group
      %th Status
  - @tasks.each do |task|
    %tbody
      %tr
        %td= task.name
        %td= task.description
        %td= task.group
        %td= task.completed ? 'completed' : 'uncompleted'
